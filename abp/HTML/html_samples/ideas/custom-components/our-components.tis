
class MyComponent: Behavior
{
  function attached()
  {
    // build initial content
    this.$content(Hello <b>world</b>!);
  }
  function onMouse(evt)
  {
    // handle some events:
    if(evt.type == Event.MOUSE_DOWN)
      view.msgbox(#information,"Got mouse down!");
      
  }
}